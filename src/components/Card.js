import React from "react";

const Card = ({movie}) => {
    while(i < 8){
        if(movie === undefined){
            continue;
        }
    }


    // if(movie === undefined){
    //     continue;
    // } else {
    //     return(
    //         <div className="card">
    //             {/* <h1>{movie.title}</h1> */}
    //             {/* <img src={movie.poster_path} alt={movie.id}></img> */}
    //             <h1>{console.log(movie)}</h1>
                
    //         </div>
    //     )
    // }
}

export default Card;